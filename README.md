# @narada2/agent-tui

Standalone repository for the Narada terminal UI carrier.

The Rust crate remains `narada-agent-tui`; this repository/package is published and referenced as `@narada2/agent-tui` / `narada2/agent-tui`.

## Commands

```powershell
cargo test
cargo build
```
